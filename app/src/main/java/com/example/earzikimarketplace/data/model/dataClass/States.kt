package com.example.earzikimarketplace.data.model.dataClass

/**
 * Enum representing different states of the UI, used on the marketplace.
 * EMPTY at first load.
 * LOADING when new content is loading
 * CONTENT when content is displayed
 */
enum class UiState {
    LOADING, CONTENT, EMPTY
}